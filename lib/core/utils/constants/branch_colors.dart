import 'package:flutter/material.dart';

/// Branch management specific colors following the design specifications
class BranchColors {
  // Primary colors
  static const Color primaryRed = Color(0xFFBF1E2D);
  static const Color primaryBlue = Color(0xFF0A3A7E);

  // Background colors
  static const Color veryLightBlue = Color(0xFFEAF0FA);
  static const Color lightBlue = Color(0xFFEDF2FA);

  // Border colors
  static const Color fieldBorder = Color(0xFFCAD5E8);
  static const Color cardBorder = Color(0xFFD7DEE8);

  // Text colors
  static const Color textSecondary = Color(0xFF6E7A8A);
  static const Color textHint = Color(0xFFB0B9C6);
  static const Color white = Color(0xFFFFFFFF);

  // Status/Feedback colors
  static const Color shadow = Color(0xFFC05050);

  // Tab bar colors
  static const Color tabInactive = Color(0xFFAAB5C3);
}

/// Global spacing system for branch management
class BranchSpacing {
  static const double xs = 4.0;
  static const double sm = 8.0;
  static const double md = 12.0;
  static const double lg = 16.0;
  static const double xl = 24.0;
  static const double xxl = 32.0;

  // Specific measurements
  static const double appBarHeight = 56.0;
  static const double fieldHeight = 48.0;
  static const double buttonHeight = 48.0;
  static const double cardHeight = 118.0;
  static const double workingDayRowHeight = 64.0;
  static const double mapHeight = 200.0;
  static const double tabBarHeight = 72.0;
  static const double imageSize = 96.0;
  static const double addButtonHeight = 32.0;
  static const double addButtonMinWidth = 88.0;

  // Border radius
  static const double borderRadius = 8.0;
  static const double buttonBorderRadius = 10.0;
  static const double addButtonBorderRadius = 20.0;
}
